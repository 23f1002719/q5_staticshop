name: Simple Workflow

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 23f1002719@ds.study.iitm.ac.in
        run: echo "Hello from GitHub Actions!"

      - name: Final Step
        run: echo "Workflow completed successfully."
